<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

   <LinearLayout
       android:id="@+id/toolbar_detail_food"
       android:layout_width="match_parent"
       android:orientation="horizontal"
       android:layout_height="?attr/actionBarSize"
       tools:ignore="UseCompoundDrawables">
       <ImageView
           android:id="@+id/btn_back_detail_food"
           android:src="@drawable/ic_back"
           android:layout_width="40dp"
           android:layout_gravity="center"
           android:layout_marginStart="14dp"
           android:layout_height="30dp"
           android:contentDescription="@string/text_img_content" />
       <TextView
           android:id="@+id/tv_title_detail_food"
           android:textAppearance="@style/title_detail_food"
           android:layout_width="match_parent"
           android:gravity="center|start"
           android:paddingStart="5dp"
           android:layout_height="?attr/actionBarSize"
           tools:ignore="RtlSymmetry" />
   </LinearLayout>
    <!-- Nội dung chính -->
    <WebView
        android:id="@+id/wv_detail_food"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_behavior="@string/appbar_scrolling_view_behavior"/>

</LinearLayout>